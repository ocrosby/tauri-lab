{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capabilities for the main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
