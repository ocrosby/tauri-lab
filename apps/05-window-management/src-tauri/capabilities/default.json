{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capabilities for both windows (main + child)",
  "windows": ["main", "child"],
  "permissions": ["core:default"]
}
