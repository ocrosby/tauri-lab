# shared/icons/

The source icon shared by every demo. `scripts/generate-icons.sh` reads `source.png` (or an image you pass on the command line) and calls `cargo tauri icon` per demo to produce the per-platform variants.

Nothing here is committed. Run the script to populate.
